type Props = {
  className?: string
}

export default function Logo({ className = "" }: Props) {
  // This file is intentionally a no-op placeholder. We migrated to LogoImg.
  // Keeping a stub here prevents build errors while we iterate.
  return null as any
}
